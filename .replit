run = "uvicorn src.serving.app:app --host 0.0.0.0 --port ${PORT:-8000}"

[nix]
channel = "stable-23_11"

[deployment]
run = ["sh", "-c", "uvicorn src.serving.app:app --host 0.0.0.0 --port $PORT"]
deploymentTarget = "cloudrun"
